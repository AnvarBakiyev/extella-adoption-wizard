# Handoff для Codex: блокер выпуска — кодоген PDF-шага без PDF-библиотеки

Статус: **единственный красный тест единой internal-RC**. Всё остальное зелёное
(preflight полный, смоук 19/19, валидатор сессий 0, обновление поверх живой машины).
После зелёного E2E выпуск завершается за ~15 минут (merge в main + пин SHA в install-all.sh).

## Твоя новая база

Ответвляйся от `origin/integration/unified-internal-rc` (**7760cef**), НЕ от `codex/prod-hardening`:
там твой `52eb41b` + 4 интеграционных фикса, найденных живым прогоном:

| Коммит | Что | Почему |
|---|---|---|
| `91cec72` | deploy.sh кладёт `system_experts/` 4/4 | дев-деплой не знал про твой локальный bundle → `local_system_expert_failed` на create-сессии |
| `fd0c417` | release.sh: снят тотальный запрет релиза из main | ломал канонический релиз владельца; твой полный preflight в релизе сохранён; пуш main из codex/* по-прежнему невозможен (скрипт пушит только текущую ветку) |
| `deafa2d` | фактическая ошибка `_post_agent` доезжает до журнала | «(error)» без причины — два E2E расследовались вслепую; это дало диагноз 404 |
| `7760cef` | мёртвый per-session агент (404) → фолбэк на живого Qwen перед стройкой | сессии штамповали удалённого агента из реестра → вся стройка падала ложным «builder_defect» |

## Блокер: симптом

`scripts/live_wizard_agentic_e2e.py` (Excel+PDF сверка пломб) падает на шаге t2:

```
agentic_run_failed · attempts 3 · failure_kind runtime
status не success: error; нет доказательства обработки файлов: registry.xlsx;
processed_files меньше числа входов
```

Репро: `python3 scripts/live_wizard_agentic_e2e.py` (мост 5.26 живой, bundle разложен).
Диагностика сохранена: сессия `wz_20260722_e74aa8`, build `build_20260722_1945_b660`
(`~/extella_wizard/runs/build_20260722_1945_b660/process/steps/t2/v1/solution_attempt_{1..3}/step_result.json`).

## Причина (подтверждена артефактами)

t1 (Excel) собирается и публикуется. t2 (PDF): Qwen генерит извлечение текста
**`basic_regex` по сырым байтам PDF без PDF-библиотеки** (`include(...)` в коде нет вовсе —
см. step_result: `"extraction_methods": ["basic_regex"]`, `files_used: [certificate.pdf]`,
0 записей). PDF от reportlab сжат (FlateDecode) → regex по байтам всегда 0. Все 3 попытки
ремонта повторяют тот же приём — «урок» не эскалирует к смене инструмента.

## Ожидаемое направление фикса (твой контур — builder prompt/repair)

1. В `_build_prompt`/`_code_artifact_prompt` (ui/wz_agentic.py): для входов `.pdf` —
   ЖЁСТКОЕ требование извлекать текст библиотекой через канон зависимостей
   `include("import pdfminer.six | pypdf | PyPDF2", ["extella-pip install …"])`;
   regex по сырым байтам PDF — запрещённый приём (объяснить почему: сжатые стримы).
2. Repair-урок: если приёмка отклонила с «0 записей из .pdf» — следующий ремонт обязан
   СМЕНИТЬ метод извлечения (библиотека), а не полировать тот же regex; иначе budget
   сгорает на идентичных попытках.
3. Регресс в preflight: сгенерённый код шага с `.pdf`-входом без PDF-библиотеки в
   `include` — не проходит (статическая проверка кода draft'а, аналог твоих lint-гейтов).

Верификация: `live_wizard_agentic_e2e.py` зелёный **дважды подряд** (кодоген стохастичен).

## Замечания в твой бэклог (не блокеры)

- Попытки native/fallback идут подряд без бэкоффа — 20-секундный флап платформы гасит весь budget.
- `agent_id`/`agent_name` сессии внести в docs/SESSION_SCHEMA.md (валидатор ворчит).
- Реестр агентов (`wz_agents_index`/`wz_current_agent` в KV) не чистится при удалении агента
  с платформы — источник мёртвых ссылок (фолбэк 7760cef прикрывает стройку, но чистка нужна).

## Пост-фикс (23.07): гэп пиннинга схемы сводки

t2-fix принят, E2E проходит. Найден следующий (НЕ блокер, в бэклог): сгенерённые процессы
отдают summary с ДРЕЙФУЮЩЕЙ раскладкой между прогонами (плоские matched/only_excel vs вложенный
verdict_distribution) — блюпринт ключи не объявляет, приёмка их не пиннит. Потребители сводки
(доставка/виджеты/скрипты) требуют стабильных ключей. Направление: Task Contract объявляет
обязательные ключи summary → приёмка enforce'ит → ремонт чинит раскладку. E2E-ассерт временно
проверяет семантику (обе формы) — после пиннинга вернуть строгий.

## Класс отказа D (23.07, прогон 4/4): кодоген уходит в «LLM внутри шага» с выдуманным endpoint

Стохастика сходимости: на слитой сборке 4 живых прогона → 3 зелёных (семантически), 1 красный.
Красный (build_20260723_1313_39d3, t2): att1 — сгенерённый код зовёт LLM ИЗНУТРИ шага по
несуществующему `https://api.extella.ai/v1/chat/completions` → 404 (endpoint галлюцинирован;
платформенный путь — /api/agent/run, но детерминированному file-шагу LLM внутри вообще не нужен);
att3 — хардкод значений образца A-101 (твой анти-хардкод гейт поймал, урок применён); att4 —
приёмка не сошлась, budget исчерпан. Fail-closed отработал честно.

Направление: в builder prompt для детерминированных file-шагов ЗАПРЕТИТЬ вызовы LLM изнутри кода
(вся семантика — на этапе генерации; шаг обязан быть детерминированным), а repair-урок «404 на
/v1/chat/completions» должен эскалировать к смене подхода, не к правке URL. Цель — сходимость
≥ 2 подряд зелёных E2E (сейчас ~75% на прогон).
