# Визард Extella — план «навести порядок и собрать красиво целиком»

Статус: v1 от 07.07.2026. Автор — сессия keyless-Qwen; согласован с сессией-тулбар
(knowledge grounding + identity + самодостаточный UI, репо-тег v1.2.0 / мост 3.53).
Единый ориентир для ОБЕИХ сессий, чтобы не расходиться. Не кодить вперёд плана.

**Источник правды по коду — ЭТОТ git-репозиторий + стабильный локальный клон (вне iCloud).**
iCloud — только доки/бэкап (решение 07.07 после сбоя iCloud-синка).

---

## Корень проблемы
Один `server.py` и эксперты жили одновременно в 4 мутабельных местах (платформа / репо /
задеплоенный мост / локальные копии) + 2 Claude-сессии параллельно → риск тихого расхождения.
Самое ценное во всём плане — **Фаза 0 + Фаза 2: один источник правды (репо) + инструмент синка + дисциплина.**

## Фаза 0 — Единый источник правды  ✅ ВЫПОЛНЕНО (07.07)
- Сверено: история репо **линейная**, тулбар-сессия легла поверх keyless-коммитов; в `server.py`
  присутствуют ОБА набора (keyless-Qwen + knowledge/identity). **Ничего не потеряно.**
- Репо + живой мост + задеплоенный `~/extella_wizard/app` = **3.53**, согласованы.
- Канон закреплён: **git-репо = источник правды**, стабильный клон `~/Downloads/extella-adoption-wizard`
  (вне iCloud). Деплой = pull из репо; платформенные эксперты = push из репо.
- Осталось (мелочь): подтянуть отставшие локальные копии 3.47→3.53 (или просто перейти на клон).

## Фаза 1 — Разобрать монолит моста
`server.py` — god-файл (HTTP + стройка + кодоген + LLM-роутинг + релиз/апдейт + Telegram + источники + сейф).
- Швы: роутинг · пайплайн стройки (`_run_build/_build_one/_audit`/оркестратор/kp-стадии) ·
  LLM-роутинг (`qwen_agent/_gen_identity`) · клиент платформы · ops · коннекторы/источники.
- Выносить модулями инкрементально под тестами. Цель: `server.py` = тонкий роутер, логика в `bridge/`.
- Канон — в код: keyless-Qwen, хартбит-`time:`, ретрай-500, `global:true` — именованные хелперы.

## Фаза 2 — Синхронизация платформа↔GitHub
- `sync.py pull` (платформа `wz_*/kp_*` → `experts/`) и `sync.py push` (репо → платформа) с манифестом (имя, хеш, cspl).
- Дрейф-чек перед пушем. Одно направление: репо канон, руками на платформе не править.

## Фаза 3 — Гигиена и структура репо
- Мусор вон: `server.py.bak_*`, скретчпад-пути в `builder_exam/*.json`, `.DS_Store`; расширить `.gitignore`.
- Раскладка: `/bridge` · `/experts` · `/concepts` · `/rules` · `/agents` · `/ui` · `/catalog` · `/docs` · `/scripts`.
- Одна схема версий: `BRIDGE_VERSION` vs репо-тег → semver-теги, версия моста производная; `CHANGELOG.md`.

## Фаза 4 — Связность UX
- `wizard.html` (hero-обложки, кабинет, подключения, мониторинг) → один язык (брендбук
  Gold/Ink/Petrol/Paper, DM Sans/JetBrains Mono), единые компоненты, честные состояния, адаптив.
- Поток как один продукт: интервью → blueprint → прогресс → кабинет → карточка витрины.

## Фаза 5 — Верификационный харнесс  ✅ ВЫПОЛНЕНО (07.07)
- `scripts/smoke.py` — сквозной keyless-круг через живой мост: health → сессия+8 ответов → образец →
  blueprint (success, стадий≥3) → стройка (→built, аудит allow, ≥1 эксперт). Exit 0/1 для CI.
  Зелёный на v3.53 (blueprint 5 стадий/63с, стройка built/280с). Гонять ПЕРЕД релизом и ПОСЛЕ
  рефакторинга (Фаза 1/4). Расширяемо: numeric-эталон 26 000 000, knowledge-стадия, публикация в витрину.

## Фаза 6 — Хвосты
- «Изменить через Строителя» — настоящий хендофф. Косметика `capabilities`.
- Платформе (Тимуру): общий keyless-Qwen на запуск; кросс-аккаунт хостинг.

---

## Порядок: 0 ✅ → 3 ✅ → 2 ✅ → 5 ✅ → 1 → 4 → 6

## Реестр рисков/вопросов
- [x] Ф0: keyless-Qwen фикс НЕ потерян — подтверждено.
- [ ] Ф2: формат манифеста синка (хеш vs версия).
- [ ] Платформа (Тимуру): общий keyless-Qwen agent_id; кросс-аккаунт исполнение на общем таргете.
- [ ] Секреты на ротацию: git PAT, токен/PIN/SSH коллеги.
- [ ] iCloud-синк папки «0. Dronor» был нестабилен 07.07 — код держим в git, не в iCloud.
